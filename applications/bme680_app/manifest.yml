# This file goes in the flipper-application-catalog repo, NOT in your app repo.
# Place it at: applications/bme680_iaq/manifest.yml
#
# Before submitting:
# 1. Replace YOUR_USERNAME with your GitHub username
# 2. Replace YOUR_COMMIT_SHA with your latest commit hash
#    (run: git rev-parse HEAD)

sourcecode:
  type: git
  location:
    origin: https://github.com/YOUR_USERNAME/bme680_app.git
    commit_sha: YOUR_COMMIT_SHA
